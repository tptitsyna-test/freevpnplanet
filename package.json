{
  "name": "freevpnplanet-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright E2E tests for Planet VPN checkout flows",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.0",
    "@types/node": "^26.0.1"
  }
}
